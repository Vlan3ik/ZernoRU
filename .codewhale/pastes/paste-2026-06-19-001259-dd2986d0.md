
1. Сделать нормальное, понятное и реальное сравнение тарифов.
2. Переделать блоки на главной странице — убрать непонятные “Индексы”, “Комментарии”, “Ж/д тарифы” в текущем виде.
3. Проверить страницы цен, особенно:
   http://localhost:18081/prices/pw-3
   Данные должны идти из БД/API, а график и архив должны показывать правдоподобные колебания цены.
4. Русифицировать карточки объектов в админке:
   http://localhost:18081/admin/seller-applications
   Сейчас в drawer/card показываются поля companyName, userName и другие английские ключи. Так быть не должно.
5. Исправить справочники:
   - при добавлении справочника всё должно быть на русском
   - добавленный регион должен реально появляться в местах выбора региона
   - статусы справочников должны быть едиными, без смеси “active”, “Проверено”, “Проверенные”
6. После правок прогнать сборки и тесты.

--------------------------------------------------
КОНТЕКСТ ИЗ НАЙДЕННЫХ ФАЙЛОВ
--------------------------------------------------

Главная страница:
- FrontEnd/src/pages/HomePage.tsx

Текущая проблема:
- Есть непонятные блоки:
  - “Индексы”
  - “Логистика”
  - “Рыночные ориентиры и комментарии”
  - повтор индексов и комментариев
- Часть значений захардкожена прямо на фронте.
- Нужно заменить на понятные бизнес-блоки из данных приложения.

Страницы цен:
- FrontEnd/src/pages/PricesPage.tsx
- FrontEnd/src/utils/price.ts
- FrontEnd/src/services/portalApi.ts
- FrontEnd/src/store/appStore.ts
- Backend/src/Zerno.Api/Program.cs
- Backend/src/Zerno.Domain/Analytics/PriceRecord.cs
- Backend/src/Zerno.Infrastructure/Services/PortalSnapshotService.cs
- Backend/src/Zerno.Infrastructure/Services/PortalSeeder.cs

Текущая проблема:
- Страница `/prices/:slug`, например `/prices/pw-3`, берёт текущую цену из snapshot/API/БД.
- Но график строится на фронте функцией `buildTrendSeries(record)`.
- Это не архив из API.
- Нужно сделать backend endpoint истории цены и подключить его к странице.

Админка:
- FrontEnd/src/pages/AdminPage.tsx

Текущая проблема:
- Drawer “Карточка объекта” рендерит:
  Object.entries(selected).map(([key, value]) => ({ label: key }))
- Из-за этого видны английские ключи:
  companyName
  userName
  createdAt
  sellerVerificationStatus
  isVerifiedSeller
  и другие.
- Нужно централизованно русифицировать labels и значения.

Справочники:
- FrontEnd/src/pages/AdminPage.tsx
- FrontEnd/src/pages/ReferenceCatalogPage.tsx
- FrontEnd/src/store/appStore.ts
- FrontEnd/src/app/AppRouter.tsx
- Backend/src/Zerno.Api/Program.cs
- Backend/src/Zerno.Infrastructure/Services/PortalSeeder.cs
- Backend/src/Zerno.Infrastructure/Services/PortalSnapshotService.cs

Текущая проблема:
- В модалке добавления справочника категории на английском:
  cultures, regions, classes, basis, lot-types, payments, delivery, subscription-tariffs
- Статусы:
  active / hidden
- В seed-данных встречается “Проверено”.
- Пользователь добавил регион, но выбрать его нигде нельзя.
- Нужно подключить справочник regions к реальным select-ам.

Тарифы:
- FrontEnd/src/pages/SubscriptionPage.tsx
- FrontEnd/src/services/portalApi.ts
- Backend/src/Zerno.Api/Program.cs

Текущая проблема:
- Endpoint `/api/analytics/tariffs` отдаёт статичные тарифы и comparison.
- Таблица сравнения выглядит нереально и слишком общей.
- Нужно сделать внятную тарифную матрицу: что реально доступно на каждом тарифе.

--------------------------------------------------
ФАЙЛЫ СМОТРЕТЬ В ПЕРВУЮ ОЧЕРЕДЬ
--------------------------------------------------

1. Backend/src/Zerno.Api/Program.cs
2. FrontEnd/src/pages/SubscriptionPage.tsx
3. FrontEnd/src/pages/HomePage.tsx
4. FrontEnd/src/pages/PricesPage.tsx
5. FrontEnd/src/services/portalApi.ts
6. FrontEnd/src/store/appStore.ts
7. FrontEnd/src/pages/AdminPage.tsx
8. FrontEnd/src/pages/MarketplaceCreateLotPage.tsx
9. FrontEnd/src/pages/MarketplacePage.tsx
10. FrontEnd/src/pages/AuthPage.tsx
11. FrontEnd/src/pages/CabinetPage.tsx
12. FrontEnd/src/pages/ReferenceCatalogPage.tsx
13. Backend/src/Zerno.Infrastructure/Services/PortalSeeder.cs
14. Backend/src/Zerno.Infrastructure/Services/PortalSnapshotService.cs

--------------------------------------------------
ПОШАГОВЫЙ ПЛАН РЕАЛИЗАЦИИ
--------------------------------------------------

ШАГ 1. Тарифы аналитики

Файл:
- Backend/src/Zerno.Api/Program.cs

Endpoint:
- GET /api/analytics/tariffs

Нужно заменить текущие plans/comparison на более реалистичную матрицу.

Пример логики тарифов:

Базовый:
- Для фермеров и небольших хозяйств.
- Культуры: пшеница, кукуруза, ячмень.
- Регионы: до 3 регионов.
- Архив цен: 30 дней.
- Графики: текущая динамика.
- Обзоры: 1 раз в неделю.
- Сигналы спроса/предложения: нет.
- Прогноз цены: краткий вывод.
- Уведомления: личный кабинет.
- Выгрузка: нет.
- Пользователи: 1.

Профессиональный:
- Для трейдеров, закупщиков, продавцов.
- Все культуры.
- Все регионы и базисы.
- Архив цен: 12 месяцев.
- Графики: сравнение периодов.
- Обзоры: ежедневно.
- Сигналы спроса/предложения: да.
- Прогноз цены: по культуре и региону.
- Уведомления: ЛК + email.
- Выгрузка: Excel/PDF.
- Пользователи: до 5.

Корпоративный:
- Для команд, экспортёров, переработчиков.
- Все культуры + свои группы.
- Все регионы + индивидуальные направления.
- Полный архив по договору.
- Сравнение регионов и экспортных базисов.
- Спецобзоры под компанию.
- Сигналы с настройкой порогов.
- Индивидуальная модель прогноза.
- ЛК + email + интеграции.
- Excel/PDF + API.
- Пользователи по договору.
- SLA, выделенный менеджер.

Frontend:
- FrontEnd/src/pages/SubscriptionPage.tsx
- Убедиться, что таблица отображает эти значения нормально.
- Не показывать непонятные “Да/Нет” везде, где нужны конкретные лимиты.

--------------------------------------------------

ШАГ 2. Главная страница

Файл:
- FrontEnd/src/pages/HomePage.tsx

Убрать или переработать непонятные блоки:
- “Индексы”
- “Рыночные ориентиры и комментарии”
- повторяющиеся блоки с захардкоженными значениями

Сделать понятные блоки:

1. Hero:
- “Рынок зерна сегодня”
- CTA: “Смотреть предложения”, “Смотреть цены”, “Смотреть аналитику”

2. “Сегодня на рынке”
- 3–4 карточки из `prices`
- культура
- регион
- текущая цена
- недельное изменение
- переход на `/prices/:slug`

3. “Свежие предложения”
- grainLots/equipmentLots
- цена, регион, проверенный продавец
- переход в маркетплейс

4. “Логистика и маршруты”
- брать данные из `referenceCatalogs['routes']`
- если пусто — не показывать блок или показывать нормальный Empty
- без непонятных индексов

5. “Аналитика и прогнозы”
- объяснить, что доступно в тарифах
- кнопка на `/analytics/tariffs`

6. “Проверка участников”
- верификация продавцов
- безопасные сделки
- аукционы только для верифицированных

Не оставлять статичные цены вроде:
- “Пшеница 3 класса: 16 840 ₽/т”
если они не берутся из `prices`.

--------------------------------------------------

ШАГ 3. История цен и графики

Backend:
- Backend/src/Zerno.Api/Program.cs

Добавить endpoint:
GET /api/prices/{priceId:guid}/history

Он должен:
- находить PriceRecord в БД
- если не найден — 404
- строить 30 точек истории цены на основе текущей записи БД
- данные могут быть тестовыми, но должны правдоподобно колебаться вокруг текущей цены
- не делать одинаковую прямую линию
- вернуть:
  {
    id,
    culture,
    region,
    currentPrice,
    weekChange,
    points: [
      { date: "2026-06-01", price: 16200 },
      ...
    ]
  }

Важно:
- История должна основываться на записи из БД, а не на фронтовой заглушке.
- Можно генерировать deterministic series от priceId, currentPrice, weekChange.

Добавить DTO/records в Program.cs или contracts:
- PriceHistoryPointDto
- PriceHistoryResponseDto

Frontend:
- FrontEnd/src/services/portalApi.ts

Добавить:
- PriceHistoryPointDto
- PriceHistoryResponseDto
- getPriceHistory(priceId: string)

Frontend:
- FrontEnd/src/pages/PricesPage.tsx

Переделать PriceDetailPage:
- убрать использование `buildTrendSeries(row)` для финального графика
- при открытии `/prices/:slug` найти row через slug
- вызвать `portalApi.getPriceHistory(row.id)`
- показать loading/error
- график строить по `history.points`
- архив цен/таблица тоже должна использовать эти points
- если API недоступен, можно показать fallback, но явно и аккуратно

Добавить на detail:
- карточку “Архив цен за 30 дней”
- таблицу:
  дата / цена / изменение к предыдущему дню
- график:
  дата / цена

Проверить:
- `/prices/pw-3`
- `/prices/archive`
- `/prices/regions`

Если `/prices/archive` сейчас не отдельная страница, либо добавить нормальную обработку, либо кнопку не должна вести в пустоту.

--------------------------------------------------

ШАГ 4. Русификация карточки объекта в админке

Файл:
- FrontEnd/src/pages/AdminPage.tsx

Проблемное место:
Drawer title="Карточка объекта"

Сейчас:
label: key

Нужно:
1. Добавить словарь labels:
   const adminFieldLabels: Record<string, string> = {
     id: 'ID',
     userId: 'ID пользователя',
     userName: 'Пользователь',
     displayName: 'Имя / название',
     email: 'Email',
     role: 'Роль',
     companyName: 'Компания',
     inn: 'ИНН',
     ogrn: 'ОГРН',
     docPhotoUrl: 'Документ',
     status: 'Статус',
     sellerVerificationStatus: 'Статус верификации',
     isVerifiedSeller: 'Продавец проверен',
     createdAt: 'Создано',
     createdAtUtc: 'Создано',
     updatedAt: 'Обновлено',
     updatedAtUtc: 'Обновлено',
     submittedAt: 'Дата подачи',
     submittedAtUtc: 'Дата подачи',
     reviewedAt: 'Дата проверки',
     reviewerName: 'Проверил',
     title: 'Название',
     description: 'Описание',
     category: 'Категория',
     region: 'Регион',
     price: 'Цена',
     amount: 'Сумма',
     culture: 'Культура',
     className: 'Класс',
     basis: 'Базис',
     source: 'Источник',
     slug: 'Код',
     summary: 'Краткое описание',
     details: 'Описание',
     contacts: 'Контакты'
   }

2. Добавить функцию:
   function adminFieldLabel(key: string): string

3. Добавить функцию форматирования значения:
   function renderAdminFieldValue(key: string, value: unknown)

Форматировать:
- даты через formatDate
- статусы через statusTag
- boolean:
  true → Да
  false → Нет
- role:
  Admin → Администратор
  Seller → Продавец
  Buyer → Покупатель
- category:
  grain → Зерно
  equipment → Техника
  service → Услуги
- plan:
  Basic → Базовый
  Professional → Профессиональный
  Corporate → Корпоративный
- пустые значения → —

4. В Drawer использовать:
   label: adminFieldLabel(key)
   children: renderAdminFieldValue(key, value)

5. Проверить страницу:
   /admin/seller-applications
   В карточке не должно быть companyName/userName/status raw keys.

--------------------------------------------------

ШАГ 5. Справочники и регионы

Файлы:
- FrontEnd/src/pages/AdminPage.tsx
- FrontEnd/src/store/appStore.ts
- FrontEnd/src/pages/MarketplaceCreateLotPage.tsx
- FrontEnd/src/pages/MarketplacePage.tsx
- FrontEnd/src/pages/PricesPage.tsx
- FrontEnd/src/pages/AuthPage.tsx
- FrontEnd/src/pages/CabinetPage.tsx
- Backend/src/Zerno.Api/Program.cs
- Backend/src/Zerno.Infrastructure/Services/PortalSeeder.cs

5.1. Русифицировать категории справочника в AdminPage

В модалке “Справочник” заменить options с английских labels на русские:

value: cultures, label: Культуры
value: regions, label: Регионы
value: classes, label: Классы зерна
value: basis, label: Базисы поставки
value: lot-types, label: Типы лотов
value: payments, label: Способы оплаты
value: delivery, label: Доставка
value: subscription-tariffs, label: Тарифы подписки
value: routes, label: Маршруты
value: rail-tariffs, label: Ж/д тарифы

Value оставлять английским кодом, label показывать русским.

5.2. Унифицировать статусы справочников

В UI:
- active → Активен
- hidden → Скрыт

В seed/backend:
- по возможности заменить Status = "Проверено" на Status = "active"
- если нельзя мигрировать, statusTag должен корректно показывать “Проверено” как “Активен” или “Проверено”, но без смеси “проверенные/активен” в одном списке.
- Лучше привести всё к code: active/hidden, отображать labelRu.

5.3. Добавить regions в загрузку справочников

Файл:
- FrontEnd/src/store/appStore.ts

Проверить `referenceCategories`.
Если там нет `regions`, добавить:
- "regions"

Иначе добавленный регион не будет загружаться как справочник.

5.4. Сделать единый источник регионов на frontend

Добавить helper:
- либо в appStore/utility
- либо локально в нужных страницах

Логика:
- брать `referenceCatalogs['regions']`
- использовать item.title как label/value
- если справочник пустой, fallback на регионы из текущих данных

Подключить в формах/фильтрах:

- PricesPage.tsx:
  фильтр “Регион” должен включать регионы из справочника + регионы из price rows.

- MarketplacePage.tsx:
  фильтр “Регион” должен включать справочник regions + регионы из лотов/услуг.

- MarketplaceCreateLotPage.tsx:
  select региона должен включать справочник regions.
  Сейчас там, вероятно, есть const regionOptions = [...]
  Нужно расширить или заменить.

- AuthPage.tsx:
  регистрация: поле “Регион” лучше сделать Select со справочником regions.

- CabinetPage.tsx:
  редактирование региона тоже должно использовать справочник.

- AdminPage.tsx:
  в модалках:
  Пользователь → регион
  Лот → регион
  Цена → регион
  Новость/материал → регион
  должны использовать Select с регионами, а не обычный Input, где возможно.

5.5. Добавленный регион должен реально появляться

Сценарий:
1. Админ → Справочники → добавить:
   Категория: Регионы
   Название: Новый регион
   Код: new-region
   Статус: Активен
2. После сохранения и reload/loadAll:
   - регион виден в справочниках
   - регион доступен в фильтре цен
   - регион доступен при создании лота
   - регион доступен в админской форме цены/лота/пользователя

Если API после создания справочника возвращает запись, обновить store или вызвать load() / loadAll().

--------------------------------------------------

ШАГ 6. Проверить существующий API создания справочника

Файл:
- Backend/src/Zerno.Api/Program.cs

Найти:
- POST /api/admin/references
- PATCH /api/admin/references/{id}

Проверить:
- category сохраняется как code
- title сохраняется
- slug генерируется, если пустой
- status сохраняется как active/hidden
- new regions доступны через:
  GET /api/reference/regions
- PortalSnapshotService/reference endpoints не фильтруют их неправильно

Если GET /api/reference/{category} фильтрует только “Проверено”, исправить на active/Проверено совместимо.

--------------------------------------------------

ШАГ 7. Не ломать предыдущие правки

Не ломать:
- аукционную верификацию
- ForumExpertApplication
- Admin expert applications
- миграцию ForumExpertApplications
- .gitignore

Проверить, что после правок:
- неверифицированный пользователь не может делать ставку
- верифицированный может
- заявка эксперта форума работает
- approve/reject экспертной заявки работают

--------------------------------------------------
ОГРАНИЧЕНИЯ
--------------------------------------------------

Нельзя:
- оставлять новые заглушки на фронте, если данные можно взять из БД/API
- показывать пользователю английские technical keys
- использовать “companyName”, “userName” как labels в UI
- плодить новые системы верификации
- ломать существующие URL
- удалять миграции
- коммитить bin/obj/dist/node_modules

Можно:
- использовать deterministic mock history для цен, если она строится на основе PriceRecord из БД
- оставлять английские code значения внутри API/БД, но UI должен показывать русские label

--------------------------------------------------
ПРОВЕРКИ ЗАПУСТИТЬ
--------------------------------------------------

Backend:
dotnet restore Backend/Zerno.sln
dotnet build Backend/Zerno.sln
dotnet test Backend/tests/Zerno.IntegrationTests/Zerno.IntegrationTests.csproj --no-build

Frontend:
npm --prefix FrontEnd install
npm --prefix FrontEnd run build
npm --prefix FrontEnd run test
npm --prefix FrontEnd run lint

Git:
git status --short

Проверить, что:
- нет новых bin/
- нет obj/
- нет FrontEnd/dist/
- нет node_modules/
- .gitignore работает

--------------------------------------------------
РУЧНЫЕ СЦЕНАРИИ ПРОВЕРКИ
--------------------------------------------------

1. Тарифы:
Открыть:
http://localhost:18081/analytics/tariffs

Проверить:
- сравнение тарифов понятное
- нет бессмысленной таблицы
- видны реальные ограничения по культурам, регионам, архиву, выгрузке, пользователям

2. Главная:
Открыть:
http://localhost:18081/

Проверить:
- нет странных блоков “Индексы/Комментарии” в старом виде
- блоки понятны фермеру/закупщику
- цены берутся из данных
- кнопки ведут в правильные разделы

3. Цена:
Открыть:
http://localhost:18081/prices/pw-3

Проверить:
- текущая цена из PriceRecord
- график из API history
- архив за 30 дней есть
- значения колеблются, а не ровная линия
- если API не отвечает, UI не падает

4. Админка заявок продавцов:
Открыть:
http://localhost:18081/admin/seller-applications

Проверить карточку объекта:
- нет labels companyName/userName/createdAt
- есть русские labels:
  Компания
  Пользователь
  Создано
  Статус
  ИНН
  ОГРН
- статусы на русском

5. Справочники:
Открыть:
http://localhost:18081/admin/references

Добавить регион:
- Категория: Регионы
- Название: Тестовый регион
- Код: test-region
- Статус: Активен

Проверить:
- он виден в справочнике
- он появляется в выборке региона при создании лота
- он появляется в фильтрах цен
- он появляется в админских формах, где выбирается регион

6. Регрессия:
- ставки на аукционе работают только для верифицированных
- заявки эксперта форума создаются/редактируются/отзываются
- админ может approve/reject экспертную заявку

ФИНАЛЬНЫЙ РЕЗУЛЬТАТ
Код должен быть готов к ревью:
- сборки зелёные
- тесты зелёные
- UI русифицирован
- тарифы выглядят как реальная продуктовая матрица
- цены имеют архив/график от API
- справочник регионов реально используется в формах